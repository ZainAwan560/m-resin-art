{
  "name": "m-resin-art",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install && npm --prefix admin install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\" \"npm --prefix admin run dev\"",
    "seed": "npm --prefix server run seed"
  },
  "devDependencies": {"concurrently":"^9.1.2"}
}
