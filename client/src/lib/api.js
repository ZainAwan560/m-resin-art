const PRODUCTION_API='https://m-resin-art-gmt3.vercel.app/api';const configuredApi=import.meta.env.VITE_API_URL?.trim();const baseApi=configuredApi||(import.meta.env.DEV?'http://localhost:5000/api':PRODUCTION_API);const API=baseApi.replace(/(?:\/api)+\/?$/i,'').replace(/\/+$/,'')+'/api';
export async function api(path,options={}){const headers={...(options.body instanceof FormData?{}:{'Content-Type':'application/json'}),...(options.headers||{})};const res=await fetch(`${API}${path}`,{credentials:'include',...options,headers});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.message||'Request failed');return data}
