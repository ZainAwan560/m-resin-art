import React from 'react';import {motion} from 'framer-motion';import {Link} from 'react-router-dom';import {Plus} from 'lucide-react';import {useCart} from '../context/CartContext';
export default function ProductCard({product,index=0}){const {add}=useCart();return <motion.article className="product-card" initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.15}} transition={{delay:index%4*.06,duration:.45}}><Link to={`/product/${product._id}`} className="product-img"><img src={product.images?.[0]} alt={product.name} loading="lazy"/><span>{product.category==='custom'?'CUSTOM':'HANDMADE'}</span></Link><div className="product-info"><Link to={`/product/${product._id}`}><h3>{product.name}</h3></Link><p>{product.category==='custom'?'Personalized piece':'Handcrafted resin'}</p><div className="product-bottom"><strong>Rs. {product.price.toLocaleString()}</strong><button onClick={()=>add(product)} aria-label="Add to cart"><Plus size={17}/></button></div></div></motion.article>}
