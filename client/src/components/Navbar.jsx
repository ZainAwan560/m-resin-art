import React,{useEffect,useState} from 'react';import {Link,NavLink} from 'react-router-dom';import {Menu,Search,ShoppingBag,X} from 'lucide-react';import Logo from './Logo';import {useCart} from '../context/CartContext';
export default function Navbar(){const [open,setOpen]=useState(false);const {count}=useCart();const links=[['Home','/'],['Shop','/shop'],['Custom Orders','/custom-orders'],['Our Process','/process'],['About','/about'],['Contact','/contact']];useEffect(()=>{document.body.classList.toggle('menu-open',open);return()=>document.body.classList.remove('menu-open')},[open]);useEffect(()=>{const close=e=>e.key==='Escape'&&setOpen(false);window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[]);return <header className="nav"><Link to="/" onClick={()=>setOpen(false)}><Logo/></Link><nav className={open?'nav-links open':'nav-links'}>{links.map(([t,u])=><NavLink key={u} to={u} end={u==='/'} onClick={()=>setOpen(false)}>{t}</NavLink>)}</nav><div className="nav-actions"><button className="icon-btn" aria-label="Search"><Search size={18}/></button><Link className="bag" to="/cart" onClick={()=>setOpen(false)}><ShoppingBag size={19}/><b>{count}</b></Link></div><button className="menu-btn" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></header>}
